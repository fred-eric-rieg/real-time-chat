import { WriteDirectMessageDirective } from './write-direct-message.directive';

describe('WriteDirectMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new WriteDirectMessageDirective();
    expect(directive).toBeTruthy();
  });
});
