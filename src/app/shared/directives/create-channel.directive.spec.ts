import { CreateChannelDirective } from './create-channel.directive';

describe('CreateChannelDirective', () => {
  it('should create an instance', () => {
    const directive = new CreateChannelDirective();
    expect(directive).toBeTruthy();
  });
});
