import { JoinChannelDirective } from './join-channel.directive';

describe('JoinChannelDirective', () => {
  it('should create an instance', () => {
    const directive = new JoinChannelDirective();
    expect(directive).toBeTruthy();
  });
});
